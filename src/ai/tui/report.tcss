/* Static inline report — one document, height:auto, no scroll panes */

Screen {
    layout: vertical;
    height: auto;
    overflow: hidden;
}

Screen:inline {
    border: none;
    height: auto;
    overflow: hidden;
}

#report {
    layout: vertical;
    height: auto;
    width: 100%;
    padding: 0;
}
