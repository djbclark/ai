[project]
name = "aiuse"
revision = "1.0"

# Declarations only — values live in ./.env (dotenv provider), never in git.
# https://secretspec.dev

[profiles.default]
PYPI_TOKEN = { description = "PyPI API token for publishing the aiuse package (token name: aiuse). Use as UV_PUBLISH_TOKEN / TWINE_PASSWORD with username __token__.", required = true }
